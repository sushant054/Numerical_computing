class NewtonRaphson
{
public:
    double X0 = 0;
    double prev;
    double root;
    double Tol = 0.0001;
    // NewtonRaphson(double);

    double Newton_Raphson();
    // double f(double);
    // double fdash(double);
};
